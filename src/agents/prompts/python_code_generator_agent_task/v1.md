StrategyLogic to implement: {strategy_json}.
Relevant Golden Code Templates (reuse patterns where applicable): {templates}.
Write a single Python module implementing `run_backtest(data: pl.DataFrame, config: dict) -> dict` using Polars for data prep and the project's VectorBT-based backtest interface. Respond with the raw Python source only -- no markdown fences, no prose.
