Strategy hypothesis: {hypothesis}. Verdict: {verdict}. Violations: {violations_summary}. Position-limit check performed: {position_limit_checked}. Circuit-filter check performed: {circuit_filter_checked}. Naked-options check performed: {naked_options_checked}. Produce a single JSON object with exactly this schema: {{"narrative": "string"}}. Respond with JSON only, no markdown, no prose.
