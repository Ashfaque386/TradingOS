Research directive: {directive_json}.
Market context: {context_json}.
Similar past strategies (avoid repeating their failure modes): {past_strategies}.
{rejection_feedback}Produce a StrategyLogic as a single JSON object with exactly this schema: {schema}. If asset_class is "F&O", also populate option_legs with the concrete option legs this strategy trades (each: symbol, option_type "CE" or "PE", strike, side "buy" or "sell", quantity) so the naked-options safety check can run against real declared legs -- a SOLD leg must be paired with a BOUGHT leg of the same type at the correct out-of-the-money strike (higher strike for a sold call, lower strike for a sold put), or it will be rejected as a naked position. If asset_class is "Equity", leave option_legs null. Respond with JSON only, no markdown, no prose.
