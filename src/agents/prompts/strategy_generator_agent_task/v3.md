Research directive: {directive_json}.
Market context: {context_json}.
Similar past strategies (avoid repeating their failure modes): {past_strategies}.
{rejection_feedback}Produce a StrategyLogic as a single JSON object with exactly this schema: {schema}. If asset_class is "F&O", also populate option_legs with the concrete option legs this strategy trades (each: symbol, option_type "CE" or "PE", strike, side "buy" or "sell", quantity) so the naked-options safety check can run against real declared legs -- a SOLD leg must be paired with a BOUGHT leg of the same type at the correct out-of-the-money strike (higher strike for a sold call, lower strike for a sold put), or it will be rejected as a naked position. If asset_class is "Equity", leave option_legs null. If your entry_conditions/exit_conditions/stop_loss/take_profit reference a genuinely tunable numeric threshold, also populate tunable_parameters as a JSON object mapping each parameter's name to a 3-element array of [kind, low, high] -- kind is "int" or "float", low/high are the real bounds a sweep should search within (e.g. {{"sma_window": ["int", 10, 50], "stop_loss_pct": ["float", 0.01, 0.05]}}). Use the same parameter names in your entry_conditions/exit_conditions/stop_loss/take_profit prose so the Python Code Generator Agent can read them back out consistently. Leave tunable_parameters null if this strategy has no genuinely tunable numeric threshold. Respond with JSON only, no markdown, no prose.
