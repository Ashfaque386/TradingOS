Research directive: {directive_json}.
Market context: {context_json}.
Similar past strategies (avoid repeating their failure modes): {past_strategies}.
{rejection_feedback}Produce a StrategyLogic as a single JSON object with exactly this schema: {schema}. Respond with JSON only, no markdown, no prose.
