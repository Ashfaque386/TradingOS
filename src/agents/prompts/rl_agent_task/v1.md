Algorithm: {algorithm}. Symbols: {symbols_summary}. Trigger reason: {trigger_reason}. Stability passed: {stability_passed}. Reward variance (coefficient of variation): {reward_variance_cv}. Backtest Sharpe: {backtest_sharpe}. Produce a single JSON object with exactly this schema: {{"narrative": "string"}}. Respond with JSON only, no markdown, no prose.
