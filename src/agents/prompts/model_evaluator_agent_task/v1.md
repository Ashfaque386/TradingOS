Candidate model id: {candidate_ml_model_id}. Production model id: {production_ml_model_id}. Decision: {decision}. Metric deltas: {metric_deltas_summary}. Confidence score: {confidence_score}. Produce a single JSON object with exactly this schema: {{"comparison_report": "string"}}. Respond with JSON only, no markdown, no prose.
