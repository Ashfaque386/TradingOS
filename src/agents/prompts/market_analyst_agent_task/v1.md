Research directive: {directive_json}.
Live data unavailable for: {missing_summary}. Note explicitly in your `insights` that these sources are unavailable rather than fabricating figures for them.
Produce a MarketContext as a single JSON object with exactly this schema: {schema}. Respond with JSON only, no markdown, no prose.
