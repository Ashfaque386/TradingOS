OBJECTIVE:
{objective}

AVAILABLE AGENTS (capability registry snapshot):
{capability_snapshot}

RELEVANT ORGANISATIONAL MEMORY (past plans / decisions / lessons; may be empty):
{memory}

Produce a JSON object with EXACTLY this shape:
{{
  "can_plan": true,
  "objective_classification": "<short slug, e.g. swing_research | portfolio_analysis | market_analysis>",
  "departments": ["<department names engaged>"],
  "approval_required": true,
  "tasks": [
    {{
      "key": "t1",
      "objective": "<what this task must produce>",
      "capability": "<one capability from the registry>",
      "assigned_agent": "<an agent name that declares that capability>",
      "priority": 1,
      "expected_output": "<artefact type, e.g. MarketContext | NewsDigest | ResearchContext>",
      "depends_on": []
    }}
  ]
}}

If the objective cannot be served, return {{"can_plan": false, "reason": "<why>"}} and nothing else.
