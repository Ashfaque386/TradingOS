Strategy hypothesis: {hypothesis}. Kill switch tripped: {kill_switch_tripped}. Correlation check: {correlation_summary}. Naked options check: {naked_options_summary}. Position sizing: {position_sizing_summary}. Produce a single JSON object with exactly this schema: {{"narrative": "string"}}. Respond with JSON only, no markdown, no prose.
