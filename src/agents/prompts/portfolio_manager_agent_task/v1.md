Real current strategies and their latest real backtest metrics: {strategies_summary}. Real current broker margin: available={available_margin}, used={used_margin}. Produce a single JSON object with exactly this schema: {{"weights": [{{"strategy_id": "string", "strategy_name": "string", "weight_pct": <float 0-100>}}, ...], "rationale": "string"}}. Weights across all strategies must sum to 100 or less (cash reserve is allowed). Only include strategies from the list you were given. Respond with JSON only, no markdown, no prose.
