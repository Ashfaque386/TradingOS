Strategy hypothesis: {hypothesis}. Optimization result: {optimization_summary}. Risk assessment: {risk_summary}. Recommended status: {recommended_status}. Produce a single JSON object with exactly this schema: {{"rationale": "string"}}. Respond with JSON only, no markdown, no prose.
