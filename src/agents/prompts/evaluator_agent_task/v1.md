Strategy hypothesis: {hypothesis}. Real backtested metrics: {metrics_json}. Pass threshold: Sharpe Ratio > 1.5 (the only numeric bar specified in this organization's design corpus; other metrics below are supporting context, not independent gates). Verdict: FAIL. Failure reasons: {failure_reasons}. Produce a single JSON object with exactly this schema: {{"feedback": "string"}}. Respond with JSON only, no markdown, no prose.
