Symbol: {symbol}. Model type: {model_type}. Task: {task}. Trigger reason: {trigger_reason}. Metrics: {metrics_summary}. Baseline comparison: {baseline_summary}. Produce a single JSON object with exactly this schema: {{"narrative": "string"}}. Respond with JSON only, no markdown, no prose.
