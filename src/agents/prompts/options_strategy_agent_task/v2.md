Underlying: {underlying}. Spot price: {spot_price}. Expiry: {expiry}. Research directive: {research_directive}. Real available option chain (strike, option_type, last_price, open_interest, implied_volatility per real listed contract): {option_chain_summary}. Produce a single JSON object with exactly this schema: {{"legs": [{{"strike": <float>, "option_type": "CE" | "PE", "side": "buy" | "sell", "quantity": <int>}}, ...], "rationale": "string"}}. Every strike you use must be one of the real strikes listed above. Every "sell" leg must be paired with a "buy" leg of the same option_type at a strike FURTHER from the spot price than the sold strike (higher strike for a sold CE, lower strike for a sold PE) -- a bought leg closer to spot than its sold leg does not count as a hedge and will be rejected. Respond with JSON only, no markdown, no prose.