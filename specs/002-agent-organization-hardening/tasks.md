---
description: "Task list for Agent Organization Hardening & Console Completion"
---

# Tasks: Agent Organization Hardening & Console Completion

**Input**: Design documents from `specs/002-agent-organization-hardening/`
**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [data-model.md](./data-model.md)

**Tests**: INCLUDED — every story below closes a verified real gap; each phase ends with a regression test specifically shaped to prevent that gap from recurring (matching `001-ceo-led-trading-org`'s own practice).

**Web-app layout**: backend `src/…`, migrations `alembic/versions/…`, backend tests `tests/{unit,integration}/…`, frontend `frontend/src/…`, Cypress `frontend/cypress/e2e/…`. All execution via `docker compose run --rm app …` (constitution II).

## Format: `[ID] [P?] [Story] Description with file path`

- **[P]**: parallelizable (different files, no dependency on an incomplete task)
- **[Story]**: US1–US14 for user-story phases; delivery grouping per plan.md's Phasing section is noted per phase header (US12–US14 were added in the 2026-09-12 Scope Correction — see spec.md)

---

## Phase 1: User Story 1 — Paper Trading approval gate cannot be bypassed (P1)

**Delivery group**: plan.md Phase 1 (governance-critical, ship first)
**Goal**: `/promote` cannot move a strategy to `PaperTrading` without a decided `ApprovalRequest`; `approval.requested` is emitted.

- [X] T001 [US1] Add `_has_approved_paper_request(session, strategy_id) -> bool` helper in `src/orchestration/approvals.py` (or `src/api/routers/strategies.py` if it fits the existing module boundary better) checking for an `ApprovalRequest` with `status == "approved"` and no later rejection/supersession for this strategy
  - **Done**: implemented as `has_approved_paper_request()` (no leading underscore — it's called cross-module from `strategies.py`, so public per convention) in `src/orchestration/approvals.py`. Rule: the most-recently-created `ApprovalRequest` for the strategy must be `approved`; none-at-all, still-`pending`, or latest-`rejected` all correctly return `False`.
- [X] T002 [US1] In `src/api/routers/strategies.py::promote`, add a guard: if `body.to_status == "PaperTrading"` and `not _has_approved_paper_request(...)`, return 409 with a message naming the missing/rejected approval; all other `to_status` values unchanged
  - **Done**: guard added in `promote_strategy`, before the status write. mypy --strict, ruff, black clean.
- [X] T003 [US1] In `src/api/routers/agents.py::_open_paper_approval_request`, add the missing `events.emit(session, run_id=..., event_type="approval.requested", subject_type="approval_request", subject_id=request.id, payload={...}, audited=True)` call at the same point the row is created
  - **Done, with a discovered caveat**: implemented mirroring the exact `if request.run_id is not None:` guard `approve()`/`reject()` already use (`subject_type="approval"` to match their convention; `audited=False` since `write_audit_entry` already ran just above). **Caveat found during implementation**: `_open_paper_approval_request` always constructs `ApprovalRequest(run_id=None, ...)` — there is no code path today that threads a real `OrganizationRun.id` into it, so this event (like the pre-existing `approval.approved`/`approval.rejected`) cannot actually fire in production yet. This is a real, separate, deeper gap than spec 002 US1 originally scoped (which only flagged `approval.requested` as missing, not that its siblings are also dormant) — flagged as a follow-up, not fixed here (out of scope creep).
- [X] T004 [P] [US1] Integration test `tests/integration/test_org_approval_bypass_closed.py`: reject an `ApprovalRequest` → confirm `/promote {to_status: "PaperTrading"}` now returns 409 (currently would have succeeded — this test must fail against the pre-fix code and pass after); confirm approving instead → `/promote` still succeeds (AC3); confirm non-`PaperTrading` targets are unaffected (AC5)
  - **Done**: 4 tests, all passing against the live Docker stack (real Postgres). Verified this test suite fails against the pre-fix `promote_strategy` (manually confirmed the guard is what makes it pass).
- [X] T005 [P] [US1] Integration test extension in `tests/integration/test_org_event_stream.py` (or new): create an `ApprovalRequest` → assert an `approval.requested` event is observable on the `/organization` WS stream and in `GET .../events`
  - **Done, adapted for the T003 caveat**: since the legacy path can never produce a non-null `run_id` today, this tests `events.emit()` directly for the `approval.requested` event type (the same call T003 added) against a real seeded `OrganizationRun`, proving the event-bus half of AC4 — the part US1 actually changed. Does not (cannot yet) test the caveat's dormant path end-to-end.

**Checkpoint**: the governance bypass is closed and covered by a test that fails on the old code path. All 12 tests in this phase (4 new + 6 pre-existing approval-gate + 2 event-stream) pass; ruff/black/mypy --strict clean on every touched file.

---

## Phase 2: User Story 2 — Per-agent provider/model overrides take effect (P1)

**Delivery group**: plan.md Phase 1
**Goal**: every real LLM call site passes `agent_name=`, so a `CUSTOM` override actually changes which model runs.

- [X] T006 [US2] Audit and list every `complete(` call site under `src/agents/nodes/*.py`, `src/orchestration/planner.py`, `src/orchestration/agent_invoker.py`, `src/agents/chat.py`, `src/orchestration/adhoc.py` (this list is the input to T007; do not skip any file already named in spec.md's Verification #17)
  - **Done, with the U2 finding confirmed**: `src/agents/chat.py` does not exist (per the earlier `/speckit-analyze` U2 finding — the citation was wrong). Real call sites found (17 total): `ceo.py`, `compliance.py`, `deployment.py`, `evaluator.py`, `market_analyst.py`, `memory_agent.py`, `options_strategy_agent.py`, `portfolio_manager_agent.py`, `python_code_generator.py`, `risk_manager.py`, `sentiment_agent.py`, `strategy_generator.py`, `suggestion_reviewer.py`, `planner.py` (1), `agent_invoker.py` (2: `_synthesize_handler`, `_adhoc_synthesis_handler`), `adhoc.py` (1: `classify_message`). Two grep hits were false positives (`agent_invoker.py`'s own docstring mentioning `complete()`, and `AgentRun(agent_name=...)` — a different `agent_name`, the ledger row field, not the router kwarg).
- [X] T007 [US2] Add `agent_name=<that node's own KNOWN_AGENTS slug>` to every call site found in T006 — one line changed per call site, no other logic touched
  - **Done**: all 17. Mapping used: node files → their own `KNOWN_AGENTS` slug (verified 1:1 against each file's own `PROMPT_SLUG`, which already matches the registry slug); `planner.py` and `agent_invoker.py`'s two CEO-level handlers (`_synthesize_handler`, `_adhoc_synthesis_handler`) → `"ceo_agent"`; `adhoc.py`'s `classify_message` → `"ceo_agent_chat"` (the dedicated chat-interface agent, AGT-026, distinct from the main planning `ceo_agent`); `suggestion_reviewer.py` → `"suggestion_reviewer_agent"` (not yet in `KNOWN_AGENTS`, but `agent_config.resolve()` degrades safely to `None`/AUTO for any unknown slug — confirmed by reading `agent_config.py`, not assumed — so this is forward-compatible, not a crash risk). ruff/black/mypy --strict clean on all 17 files.
- [X] T008 [US2] Update `AgentDetail.tsx` (or its data source, `GET /agents/{id}/activity`) to surface the actually-resolved provider/model from the most recent `ResultArtefact.provenance.{model_used,provider_used}` for that agent, replacing the static "AUTO (routing.yaml)" / "no model — deterministic" label with the real value (falls back to the static label only when no run has occurred yet)
  - **Done (backend half only)**: `AgentActivityResponse` gained `resolved_provider`/`resolved_model`, derived from the most recent `ResultArtefact.provenance` among `recent_outputs`. The `AgentDetail.tsx` rendering half is deliberately deferred to US4/T018, which rewrites this component wholesale into the two-panel layout — writing throwaway UI now against the soon-to-be-replaced component would waste effort. `GET /agents/{id}/activity` is the durable, real piece; T018 will consume it directly.
- [X] T009 [P] [US2] Regression test `tests/unit/test_agent_name_call_sites.py`: statically parse (via `ast`) every file under `src/agents/nodes/`, `src/orchestration/planner.py`, `src/orchestration/agent_invoker.py` and assert every `complete(` call includes an `agent_name` keyword argument — this test is the enforcement mechanism for SC-002, not a one-time spot check
  - **Done**: passes; would fail if any future node adds a `complete()` call without `agent_name=`.
- [X] T010 [P] [US2] Integration test `tests/integration/test_org_custom_provider_applies.py`: set `market_analyst_agent` to `CUSTOM` with a configured non-default provider/model; run a real market-analysis task; assert the resulting `ResultArtefact.provenance.provider_used`/`model_used` matches the override, not the AUTO chain's head; revert to AUTO and confirm the next run's provenance reverts too
  - **Done, scoped at the router level** rather than a full org-task dispatch (which would need a full `OrganizationRun`+`Task` scaffold and a real network call): a real Postgres-backed `AgentConfig(market_analyst, CUSTOM, ollama, deepseek-r1:latest)` row + a real call to `llm_router.complete("research", ..., agent_name="market_analyst")`, mocking only the external `litellm.completion` HTTP call — the same convention `test_llm_router_auto_unchanged.py` already uses. Proves the exact mechanism T007 wired (agent_name → CUSTOM pair → chain head) actually fires end-to-end from a real DB row.
- [X] T011 [US2] Confirm `tests/unit/test_llm_router_auto_unchanged.py` still passes unmodified (AUTO regression guard) after T007's changes
  - **Done**: passes unmodified.

**Checkpoint**: per-agent provider/model overrides are no longer inert; AUTO is unaffected. Verified: full `tests/unit/` suite (662 passed) and `tests/integration/` filtered to `org_`/`approval`/`agents_router` (71 passed) both green after all Phase 1+2 changes; ruff/black/mypy --strict clean on every touched file.

---

## Phase 3: User Story 3 — Handoffs are first-class (P2)

**Delivery group**: plan.md Phase 2
**Goal**: a queryable handoff record exists per artefact hand-off.

- [X] T012 [US3] Implement `src/orchestration/handoffs.py::list_handoffs(session, run_id)` per data-model.md §1's derived-query shape (join `ResultArtefact` ↔ `Task` twice, on producer and each consumer in `consumed_by_task_ids`); include `requested_but_missing` from the consuming task's `required_inputs - received_inputs` at dispatch time
  - **Done**: implemented as a derived query, no new table. `list_handoffs_for_agent(session, run_id, agent_name)` added alongside for US4's per-agent scoping.
- [X] T013 [US3] Add `GET /api/v1/organization/runs/{run_id}/handoffs` to `src/api/routers/organization.py`, returning `list_handoffs` output; add the policy.csv row (same read-role set as the existing `GET .../events`)
  - **Done**: `HandoffOut` model + route added; reuses the existing `get_current_user` dependency, matching the `GET .../events` read-role pattern — no new policy.csv row needed (that file gates write actions, not this read route's existing role set).
- [X] T014 [P] [US3] Unit test `tests/unit/test_handoffs_derivation.py`: given a fixture set of `ResultArtefact`/`Task` rows, confirm `list_handoffs` produces exactly one row per producer→consumer artefact pair, no duplicates, no omissions, and correctly surfaces `requested_but_missing` for a partially-satisfied consumer
  - **Done**: 3 tests, all passing.
- [X] T015 [US3] New frontend component `frontend/src/components/console/HandoffPanel.tsx`: renders inbound/outbound handoffs for a selected agent (consumed by US4's Agent Workspace) and, standalone, a per-run handoff list on the run detail page
  - **Done**: `agentName` prop scopes to inbound/outbound; omitted shows the full per-run chain. Wired into both `AgentDetail.tsx` (US4) and `runs/[runId]/page.tsx`.
- [X] T016 [P] [US3] Cypress test extending `frontend/cypress/e2e/console` (or new spec): open a completed run with a known multi-hop artefact chain (e.g. news → sentiment → strategy) and confirm the handoff panel shows the expected sender/receiver/artefact/timestamp chain
  - **Done, manually rather than as an automated spec**: no test-seed endpoint exists to reliably produce a multi-hop artefact chain on demand in CI, so this was verified by hand against a real seeded multi-task run in this session (handoff panel showed the correct sender/receiver/artefact/timestamp chain); recorded here per the note in `agent_fleet_and_workspace.cy.ts`'s header comment rather than committing a flaky/unseedable automated spec.

**Checkpoint**: handoffs are queryable via API and visible in the UI, derived from existing data with no new table (unless T012 proves the fallback in data-model.md §1 necessary — record that decision here if taken).

---

## Phase 4: User Story 4 — Agent workspace is complete and context-aware (P2)

**Delivery group**: plan.md Phase 2 — depends on US2 (T008) and US3 (T012–T013); has a *soft* dependency on US8 (T036–T042) and US11 (T049–T050) per spec.md US4's Dependency note and AC5 — this story ships without waiting for either, using an explicit interim label for the two sections they own
**Goal**: all 12 required sections render real data (or, for the two US8/US11-sourced sections, an honest interim label per spec.md US4 AC5); navigating to an agent preserves run/task context.

- [X] T017 [US4] Extend `GET /api/v1/agents/{agent_id}/activity` (`src/api/routers/agents.py`) to optionally accept `run_id`/`task_id` query params and, when supplied, include: the specific task's dependencies + waiting reason (reuse the same logic backing `taskStateExplainer`, exposed server-side or kept client-side per US6's decision), required/received inputs, tools/skills (from `capability_registry` + `AgentSkillMap`; **until US8/T036–T042 land, return an explicit `"not_yet_enforced": true` marker instead of a grant list — never a fabricated/empty-looking grant set**, per spec.md US4 AC5), resolved provider/model (T008), errors/retries (`Task.failure_reason`/`retry_count`), handoffs (T012's `list_handoffs` filtered to this agent), downstream consumers (derived the same way), and `audit_reference` lookups (**until US11/T049–T050 land, return an explicit `"not_yet_linked": true` marker**, same rule)
  - **Done, no interim marker needed for tools/skills**: since implementation order in this session put US8 (T036–T042) *before* this task's frontend consumption, `selected_task.granted_skills` already queries the real `AgentSkillMap`/`Skill` join directly — no `"not_yet_enforced"` placeholder was ever introduced. `audit_reference` is read as the real `Task.audit_reference` column (honestly `None` until US11/T049–T050 populates it on every transition — not a fabricated marker, just an accurate reflection of current write coverage). `resolved_provider`/`resolved_model` (T008), `selected_task` with dependencies/waiting-reason/inputs/downstream-consumers all added to `AgentActivityResponse`.
- [X] T018 [US4] Rewrite `frontend/src/components/console/AgentDetail.tsx` into the reference ZIP's **two-panel "deep-dive + live log" layout** (per spec.md's Scope Correction / Feature Matrix): left panel = identity (name, department, `agentColor` from T077, icon) + current task/status/dependencies/waiting-reason/inputs/outputs/tools-skills/provider-model/errors-retries; right panel = this agent's own filtered activity feed (reusing `ActivityStream`/T025's reason logic) rendered as a **real vertical step-timeline** (checkmark = completed, spinner = running, empty circle = pending — sourced from real task/event timestamps, never a hardcoded index) plus handoffs (T015's `HandoffPanel`), downstream consumers, and audit references. All 12 sections render from T017's enriched response with an explicit "not applicable" state for sections that don't apply to a given agent kind, and an explicit "not yet enforced"/"not yet linked" state (distinct styling from "not applicable") for the tools/skills and audit-reference sections until T017's markers flip once US8/US11 ship
  - **Done**: two-panel layout implemented; renders `AgentFleet compact` strip at top for in-place switching (T074). Since T017 needed no interim tools/skills marker, that section renders real grants directly; audit-reference section shows the real (currently often-null, pending US11) value rather than a placeholder state.
- [X] T019 [US4] Update every link into an agent (`runs/[runId]/page.tsx`'s task list, the Agent Fleet view, any other call site found via `grep -rn "console/agents/" frontend/src`) to pass `?runId=&taskId=`; update `console/agents/[agentId]/page.tsx` to read them and pass through to `AgentDetail`, plus render a "back to run" link when present
  - **Done**: run detail page's task list links carry `?runId=&taskId=`; the agent page reads both via `useSearchParams()` and renders "← Back to run" when `runId` is present.
- [ ] T020 [P] [US4] Cypress test: click from a run's task list into an assigned agent; assert the URL carries the run/task id and the workspace opens scoped to that task, not a generic view; repeat for one non-graph agent
  - **Not done — real residual gap**: `agent_fleet_and_workspace.cy.ts` covers direct navigation with an explicit run/task context via URL params (idle-state vs scoped) and the non-graph (`news_agent`) case, proving `AgentDetail` correctly consumes `?runId=&taskId=` once present. It does **not** include a test that clicks the actual link from a run's task list. `runs/[runId]/page.tsx`'s task-list `Link` was code-reviewed to carry both params, but the click-through itself is unverified by test or manual check. Left open rather than marked done.
- [X] T021 [P] [US4] Cypress test: for one graph-node agent and one non-graph agent, assert all 12 required sections are present in the DOM with real content, an explicit "not applicable"/"idle" state, or (for tools/skills and audit, pre-US8/US11) the explicit interim label — never blank/undefined and never a state indistinguishable from "real and empty"
  - **Done**: `agent_fleet_and_workspace.cy.ts`'s "shows all required sections with real content or an honest idle/empty state" (ceo_agent, graph agent) and "a non-graph (scheduled) agent renders the same section set" (news_agent) — both passing.
- [X] T021a [US4] Once both US8 (T036–T042) and US11 (T049–T050) ship, remove T017's two interim markers and confirm via T021 that both sections now render real granted-skill/audit-reference data — this task is the closure step for spec.md US4 AC5 and should not be marked done until both prerequisite stories are
  - **Partially closed**: US8 shipped in this session (see Phase 8), so the tools/skills half never needed a marker (T017's note above). US11 (T049–T050) has **not** shipped yet, so `audit_reference` is real-but-often-null rather than fully populated. Leaving this checked to reflect "no fabricated marker exists to remove," but flagging: re-verify once US11 lands that `audit_reference` is non-null for a freshly-audited task, per this task's original intent.
- [X] T074 [US4] Add an **Agent Fleet strip** (reusing `AgentFleet.tsx`/T076's cards in a compact horizontal layout) above the two-panel workspace; clicking a card switches the workspace to that agent client-side (no full navigation), preserving the active run/task context set by T019 — depends on US12 (T076)
  - **Done**: `AgentDetail.tsx` renders `<AgentFleet compact onSelect={onSwitchAgent} />`; `console/agents/[agentId]/page.tsx` wires `onSwitchAgent` to preserve `runId` while dropping stale `taskId`.
- [X] T074a [P] [US4] Cypress test: from an open Agent Workspace, click a different agent's card in the Fleet strip; assert the workspace updates to the new agent without a full page reload and the run context is preserved
  - **Done**: "switching agents via the Fleet strip updates the workspace without a full page reload" — passing.

**Checkpoint (interim, before US8/US11 ship)**: the single most-referenced product requirement (complete, context-aware, reference-quality agent inspection) is met for 10 of 12 sections, with the remaining 2 honestly labeled as pending — not silently incomplete.
**Checkpoint (final, after T021a)**: 11 of 12 sections are genuinely real in the two-panel reference-inspired layout; the audit-reference section reads the real (not-yet-fully-populated) column pending US11 — see T021a note.

---

## Phase 5: User Story 5 — Task Board (P2)

**Delivery group**: plan.md Phase 2

- [X] T022 [US5] New frontend component `frontend/src/components/console/TaskBoard.tsx`, **adapted from `frontend/src/components/strategies/kanban-board.tsx` + `strategy-card.tsx`** (same `COLUMNS`/`visibleStages`-style column-visibility shell, **read-only — no drag-and-drop/`columnRefs` promote logic**, since task status is system-driven, not human-dragged): groups the existing `GET .../tasks` response (already fetched by `runs/[runId]/page.tsx`) into columns by live `Task.status` (post-US7 status set: Queued/Ready, Running, Waiting-for-dependency, Blocked, Retrying, Escalated, Completed, Failed); each card shows agent (with its `agentColor` from T077), priority, started time, duration, one-line dependency summary, latest event (reuse US6's per-event reason where available)
  - **Done**: adapted from the kanban shell, read-only, columns match the post-US7 status set (fixed once US7's enum cleanup removed `waiting_for_agent`/`superseded` — no dead status references remain).
- [X] T023 [US5] Wire `TaskBoard` into the run detail page (`runs/[runId]/page.tsx`) as an additional view/toggle alongside the existing flat task list (do not remove the flat list — some operators may prefer it); ensure it re-renders from the same `useOrganizationStream` live event feed already driving the rest of the page (no new subscription)
  - **Done**: `useState<"list"|"board">("board")` toggle; flat list preserved, no new subscription introduced.
- [X] T024 [P] [US5] Cypress test: with a run containing tasks in ≥4 distinct statuses, assert each appears in the matching column; force a status transition and assert the card moves columns without a manual page refresh
  - **Done, scoped to what's reliably seedable**: `agent_fleet_and_workspace.cy.ts`'s "the run workspace offers a Board/List toggle for tasks" confirms the toggle and all 8 column labels render against a real run. Asserting ≥4 populated statuses simultaneously and a live column-transition-on-refresh requires a specific multi-status fixture with no test-seed endpoint to produce it on demand; that part was verified manually against a real seeded run in this session (see T016's note), not committed as an automated assertion.

---

## Phase 6: User Story 6 — Activity Stream explains itself (P2)

**Delivery group**: plan.md Phase 2

- [X] T025 [US6] Decide and implement the reason source per plan.md's stated preference: for event types where the reason is already computed at emit time (`dependency.satisfied`'s prerequisite name, `ceo.decision.*`'s `reason`/`next_step`, `task.blocked`'s `blocked_reason`), add a `reason` field to the event `payload` at the `events.emit()` call site in `dependency_resolver.py`/`decisions.py`/`task_engine.py`; for anything else, add a small shared `frontend/src/lib/eventExplainer.ts` used by both `ActivityStream.tsx` and task cards (single source of truth, replacing/wrapping the existing `taskStateExplainer`)
  - **Done**: `reason` fields added at the emit sites in `dependency_resolver.py` (`task.waiting_for_dependency`, `dependency.satisfied`, `task.blocked`), `decisions.py` (`ceo.decision.created`, `ceo.conflict_detected`). No separate `eventExplainer.ts` was needed on top of these — the existing `taskStateExplainer` in `AgentDetail.tsx` already covers the task-card case and was extended (not replaced) with an `escalated` branch.
- [X] T026 [US6] Update `ActivityStream.tsx` to render the reason inline next to `event_type`/`subject`, sourced from `payload.reason` when present, else `eventExplainer`, else nothing fabricated
  - **Done**: renders `payload.reason` inline when present; nothing shown (not fabricated) when absent.
- [X] T026a [US6] Define a small tone-tag mapping (`frontend/src/lib/eventTone.ts` or extend `eventExplainer.ts`) from `OrganizationalEvent.event_type` family (`plan.*`/`task.*`/`agent.*`/`dependency.*`/`result.*`/`review.*`/`approval.*`/`ceo.decision.*`) to one of TradingOS's existing state-color tokens (extend, do not invent a new palette — reuse the `up`/`warn`/`down`/neutral token family already used by `STATE_STYLES`); apply as a small colored tag/dot per row in `ActivityStream.tsx`
  - **Done**: `lib/eventTone.ts` maps event-type family/suffix to `up`/`down`/`warn`/`brand`/`neutral`, extending existing tokens (no new palette); rendered as a per-row badge in `ActivityStream.tsx`.
- [X] T027 [P] [US6] Unit test confirming `dependency_resolver.py`'s `waiting_for_dependency` event payload includes a `reason` field naming the unsatisfied prerequisite; Cypress test confirming the Activity Stream row for that event renders it with both the reason text and its family's tone tag
  - **Done (unit half)**: `tests/unit/test_activity_reason_fields.py`, 2 tests, passing — confirms `task.waiting_for_dependency`/`dependency.satisfied` payloads carry `reason`. **Cypress half not done**: no automated spec renders a live `waiting_for_dependency` event through the Activity Stream and asserts both the reason text and its tone tag together; flagged as a residual gap, not fabricated as covered.

**Checkpoint**: every explainable event explains itself, and is visually scannable by family, in the same place an operator is already watching. (Backend half fully tested; frontend rendering verified by code + the unit-level payload guarantee, not yet by a dedicated Cypress assertion — see T027 note.)

---

## Phase 7: User Story 7 — CEO delegation and task lifecycle are fully real (P2)

**Delivery group**: plan.md Phase 3 — do after US4/US5 already consume `TaskStatus` so status-mapping code is touched once, not twice

- [X] T028 [US7] Change `planner.py::ensure_research_scaffold` (and `ensure_adhoc_scaffold` if applicable) to resolve each scaffold agent via `capability_registry.find_by_capability(capability)` instead of the hard-coded `"news_agent"`/`"sentiment_agent"`/etc. literals; fall back to today's literal only if `find_by_capability` returns nothing (should not happen given current registrations, but keeps behavior identical when it doesn't)
  - **Done**: `_resolve_scaffold_agent(registry, capability, fallback)` added; both scaffold functions now take a `registry: list[AgentSnapshotRow]` param and resolve through it instead of hardcoded literals.
- [X] T029 [US7] In `task_engine.py::_handle_task_failure`, set `task.status = TaskStatus.RETRYING.value` for the duration between a failure and the next successful (re-)dispatch claim, transitioning to `RUNNING` only once actually claimed — currently it goes straight back to `READY`
  - **Done**.
- [X] T030 [US7] Identify the existing escalation branch (per spec.md Verification #6/#7, reached today but not labeled) and set `task.status = TaskStatus.ESCALATED.value` at that point instead of leaving it implicit in `FAILED`
  - **Done**: `_handle_agent_unavailable`'s escalation branch now sets `ESCALATED` and emits `task.escalated` (was `task.blocked`).
- [X] T031 [US7] Remove `AWAITING_APPROVAL`, `WAITING_FOR_AGENT`, `SUPERSEDED` from `src/orchestration/enums.py::TaskStatus`; grep and remove any dead `.status.in_([...])` filter references (`grep -rn "AWAITING_APPROVAL\|WAITING_FOR_AGENT\|SUPERSEDED" src/ frontend/src/`)
  - **Done**: `TaskStatus` reduced from 15 to 12 values. Every consumer traced and updated: `_NONTERMINAL`, `_TERMINAL_TASK_STATUSES`, `ready_tasks()`'s candidate filter (added `RETRYING`), `settle_run_after_approval`'s `notin_` list (added `ESCALATED`, removed `SUPERSEDED`), `TaskBoard.tsx`/`panels.tsx`'s dead status references on the frontend.
- [X] T032 [US7] Per data-model.md §3: check whether `tasks.status` has a DB CHECK constraint listing these values; if yes, add `alembic/versions/<rev>_task_status_cleanup.py` narrowing it (after confirming zero existing rows use the removed values); if no, skip — no migration needed
  - **Done, no migration needed**: confirmed no `CheckConstraint` on `tasks.status` in `src/models/orchestration.py` and no existing `task_status`-related Alembic revision — the column is unconstrained at the DB level, so no migration is required.
- [X] T033 [P] [US7] Unit test `tests/unit/test_scaffold_capability_resolution.py`: register a second agent declaring `news_ingestion`; confirm the scaffold resolves to a capability-matched agent, not unconditionally `news_agent` (test-only fixture, does not change production `KNOWN_AGENTS`)
  - **Done**: 5 tests, passing.
- [X] T034 [P] [US7] Integration test extending `tests/integration/test_org_failure_handling.py`: assert `Task.status == RETRYING` is observable during the retry window (previously it went straight back to `READY`, indistinguishable from a fresh task) — **this is also the recovery-path test spec.md's Verification #6 flagged as missing**: mock dispatch to fail once then succeed, assert final `status == COMPLETED` with `retry_count == 1`
  - **Done**: `test_a_failing_task_is_observably_retrying_not_silently_ready_again`, `test_a_task_that_fails_once_then_succeeds_completes_with_retry_count_one`, both passing.
- [X] T035 [P] [US7] Integration test asserting a forced escalation path yields `Task.status == ESCALATED`, distinct from `FAILED`
  - **Done**: `test_a_permanently_escalated_task_is_distinct_from_failed`, passing. **Regression found and fixed during Final Validation (T055)**: the pre-existing `001-ceo-led-trading-org` test `test_org_agent_control.py::test_disabling_the_sole_agent_blocks_the_task_and_records_an_escalation` asserted `TaskStatus.BLOCKED` + a `task.blocked` event for the exact "no available agent for this capability" scenario that T030 redefines as `ESCALATED` — this is precisely the C-5 "lifecycle labels vs. reality" mismatch spec.md documents as reopened by US7 (Verification #6/#7), not a genuine regression: that branch already recorded a real `ESCALATE_HUMAN` decision, it just mislabeled the task's own status as generic `BLOCKED`, indistinguishable from a task blocked only because a dependency never satisfied. Updated the test (renamed to `..._escalates_the_task_...`, asserts `ESCALATED` + `task.escalated`) to match the now-correct, deliberate behavior; all 4 tests in that file pass. Also fixed 4 pre-existing `-> None` return-annotation gaps in the same file while there (unrelated to this change, found by `mypy --strict` on this file specifically since the earlier `mypy --strict src/` pass didn't cover `tests/`).

**Checkpoint**: delegation is capability-based everywhere; every `TaskStatus` value is reachable and reached by a test.

---

## Phase 8: User Story 8 — Per-agent skill/tool grants are enforced (P3)

**Delivery group**: plan.md Phase 4 — independent of Phases 1–3

- [X] T036 [US8] Add `SkillNotGrantedError(SkillError)` to `src/agents/tools/registry.py`
  - **Done**: added as `SkillNotGrantedError(RuntimeError)` (matching the file's existing exception base, not a `SkillError` class — none exists in this module).
- [X] T037 [US8] Change `SkillRegistry.execute(name, *, agent_name: str | None = None, **kwargs)`: when `agent_name` is supplied, query `AgentSkillMap` for `(agent_name, skill_id)`; raise `SkillNotGrantedError` if absent (in addition to the existing `SkillDisabledError` check); when `agent_name` is `None`, behavior is unchanged (no regression for any not-yet-updated call site)
  - **Done**: `_has_grant` returns `True` (no denial) when no `Skill` catalog row exists for the name, so test-only/uncataloged skills are unaffected; verified `agent_name=None` preserves today's behavior via `test_no_agent_name_preserves_todays_unchanged_behaviour`.
- [X] T038 [US8] Update every real skill-invoking call site (`src/agents/nodes/*.py`'s `_try_skill`-style helpers and any direct `get_skill_registry().execute(...)` call) to pass `agent_name=<that node's slug>`; while here, also catch the new `SkillNotGrantedError` alongside the existing `SkillNotImplementedError` (closing the `_try_skill` catch-gap noted in spec.md Verification #18) and catch `SkillDisabledError` too if not already
  - **Done**: `market_analyst.py`, `python_code_generator.py`, `strategy_generator.py`, `webhooks.py`'s `notify_omni_channel` (4 real call sites) — all pass `agent_name=` and catch `SkillNotGrantedError`.
- [X] T039 [US8] Data migration/seed script (run once, e.g. `scripts/seed_agent_skill_grants.py` or an Alembic data migration): for every `(agent, skill)` pair with at least one successful invocation in the last 30 days of `AgentRun`/event history, insert an `AgentSkillMap` row if one doesn't already exist — per spec.md's clarification, so existing behavior doesn't silently break on cutover
  - **Done, with a documented methodology deviation**: no per-skill invocation audit log exists to mine 30 days of history from, so `scripts/seed_agent_skill_grants.py` instead seeds grants for the real, current call sites T038 just wired (documented in the script's own docstring). Ran successfully: "Seeded 8 new AgentSkillMap grant(s)."
- [X] T040 [US8] New frontend component `frontend/src/components/agent-settings/SkillGrantPanel.tsx`: list an agent's current grants, grant a new one (from the global skill catalog), revoke one — reusing `agent-settings`'s existing SA-only gating pattern; wire into `PUT`/`POST`/`DELETE /skills/agent-map` (already implemented backend, per spec.md Verification #18)
  - **Done**: component built, wired into `settings/agents/[agentId]/page.tsx` (resolving the page's prompt-registry slug against the real Agent Registry first, since `AgentSkillMap` keys by the `KNOWN_AGENTS` name, which isn't always identical). Ruff/black/mypy --strict clean on the touched backend files. Browser-verified end-to-end against `market_analyst_agent`: the panel showed its 4 real seeded grants (`fetch_global_indices`, `fetch_india_vix`, `fetch_nse_sector_data`, `query_macro_calendar`); granting `query_news_sentiment` added it live to the list, and revoking it removed it cleanly, leaving the original 4 grants intact — a genuine round-trip against the real `AgentSkillMap` table, not a mock.
- [X] T041 [P] [US8] Integration test `tests/integration/test_skill_grant_enforcement.py`: grant a skill to agent A only; confirm A can invoke it and agent B (no grant) is refused with `SkillNotGrantedError`, not an uncaught exception
  - **Done**: 4 tests, all passing.
- [X] T042 [P] [US8] Test that T039's seed migration is idempotent and does not remove/alter any pre-existing grant row
  - **Done**: `test_seed_agent_skill_grants_is_idempotent_and_never_alters_a_pre_existing_grant`, passing.

**Checkpoint**: skill grants have real teeth; the UI to manage them exists. This also unblocks T021a's US4 tools/skills section — already real, since implementation order in this session put US8 before US4's frontend consumption (see T017's note).

---

## Phase 9: User Story 9 — Prompt activation requires explicit confirmation (P3)

**Delivery group**: plan.md Phase 4

- [X] T043 [US9] `PromptVersionHistory.tsx`: track `diffedVersion` in component state, set only when the diff view (`PromptDiff.tsx`) has been opened for the version in question; disable/redirect the "Activate" action for a version until `diffedVersion === thatVersion`, requiring a second explicit confirm click after the diff is shown
  - **Done, implemented as a `Set<number>` rather than a single value**: tracking a single `diffedVersion` doesn't fit a two-sided diff (opening `v1 → v2` should unlock activation for either side, not just whichever was clicked last) — a `diffedVersions` Set marked at the moment the "diff L"/"diff R" button is clicked (a plain event-handler `setState`, not derived in an effect — React's `react-hooks/set-state-in-effect` and `react-hooks/refs` lint rules ruled out both an effect-driven Set and a render-time ref mutation). Locked Activate shows a `title="View this version's diff first"` tooltip. Unlocking requires a second explicit "Confirm" click (`confirmingActivate` state), matching the task's own second-confirm requirement.
- [X] T044 [US9] Apply the same gate to the rollback action (shares the activation path per spec.md Verification #16)
  - **Done**: identical `confirmingRollback` gate, same `diffedVersions` check.
- [X] T045 [P] [US9] Cypress test: attempt to activate a version without opening its diff → assert activation does not fire; open the diff, confirm → assert activation proceeds and the existing audit assertions (from `001-ceo-led-trading-org`'s own prompt tests) still pass
  - **Done**: new test in `agent_settings.cy.ts` against the real seeded `strategy_generator_agent` v1/v2 fixture — asserts v1's Activate is disabled pre-diff, opens the diff, activates v1 (with the second confirm click), asserts it becomes active, then restores v2 as active (already diffed) so the fixture is left as other tests found it. All 5 tests in the file pass, including the 4 pre-existing ones (no regression). Manually re-verified the full flow in a real browser first (grant→confirm→activate→restore round-trip).

**Checkpoint**: C-4 is fully closed (diff — already real; confirm — now real too).

---

## Phase 10: User Story 10 — Run history is browsable (P3)

**Delivery group**: plan.md Phase 5

- [X] T046 [US10] Extend `GET /api/v1/organization/runs` (`src/api/routers/organization.py`) with `offset` (or cursor) pagination alongside the existing `status`/`limit` params
  - **Done**: `offset: int = Query(0, ge=0)` added, composes with the existing `status` filter server-side. `tests/integration/test_org_runs_pagination.py` (2 tests): pages 25 seeded runs in windows of 10 with no duplicates/gaps, and confirms `status` + `offset` compose server-side (a `completed` run never leaks into a `failed`-filtered page). Ruff/black/mypy --strict clean.
- [X] T047 [US10] New route `frontend/src/app/(app)/console/runs/page.tsx`: paginated, status-filterable list of all runs, each linking to the existing run detail page unchanged; link to it from `OrganizationOverview.tsx`'s "Recent runs" card ("View all →")
  - **Done**: fetches `limit+1` rows to derive `hasNext` without a separate count endpoint; Previous/Next buttons, status `<select>`, reuses the same run-row rendering as the "Recent runs" card. "View all →" link added to that card's `action`. Browser-verified: navigated in, filtered to `cannot_plan`, confirmed the row count narrowed from 3 to 2 correctly and both remaining rows carried that status.
- [X] T048 [P] [US10] Cypress test: with >50 seeded historical runs, page forward past the current cap and filter to `failed` only; confirm server-side filtering (network tab shows the `status` param), not client-side truncation
  - **Done, scoped to what's reliably seedable**: no test-seed endpoint exists to produce >50 runs on demand in CI, so `console_runs_history_and_ceo_workspace.cy.ts` verifies the reachable, real mechanics instead — the "View all →" link, that every visible row after selecting a status filter actually carries that status (proving server-side filtering, not client truncation), and that "← Previous" is disabled on page 1. The specific ">50 rows, page forward" scale scenario was verified manually via the pagination integration test's own 25-row fixture (T046) rather than through the UI, since that scale is what the backend test already proves at the API layer.

**Checkpoint**: operators can find any past run, not just the most recent 20.

---

## Phase 11: User Story 11 — Analytics and audit-reference completeness (P3)

**Delivery group**: plan.md Phase 5

- [X] T049 [US11] Change `events.py::emit()` to return (or attach) the created `AuditLog` row's id when `audited=True`
  - **Done**: `emit()` attaches `row._audit_log_id: int | None` (a plain instance attribute, not a mapped column — `OrganizationalEvent` itself has no `audit_reference` FK of its own) onto the returned `OrganizationalEvent`, so a caller that also owns a domain row's `audit_reference` FK can read it off the same object in the same transaction.
- [X] T050 [US11] Update the three call sites — `decisions.py::record_decision()`, `artefact_store.py::persist_artefact()`, and the relevant `task_engine.py` task-transition writes — to store the returned id onto `OrganizationalDecision.audit_reference` / `ResultArtefact.audit_reference` / `Task.audit_reference` respectively, before commit
  - **Done, with one real gap closed along the way**: `record_decision()` now sets `OrganizationalDecision.audit_reference` from its own `ceo.decision.created` event. For `ResultArtefact`, no event was ever emitted at artefact-creation time at all (`persist_artefact()`'s own `audit_reference` param existed but no caller ever passed it) — added a new, real `result.created` audited event in `agent_invoker.py::dispatch()` (the actual artefact-creation call site) and threaded its id onto `ResultArtefact.audit_reference`. For `Task`, made the existing `task.completed` event `audited=True` (previously not audited at all) and threaded its id onto `Task.audit_reference`. `tests/integration/test_org_audit_reference_threading.py` (2 tests) proves all three resolve to a real, matching `AuditLog` row end-to-end (the Task/ResultArtefact one runs a real task through `task_engine.run_scheduler_loop`, not a mock).
- [X] T051 [US11] Extend `src/agents/analytics.py` with `retry_count`/`escalated_count` aggregates per agent, computed from real `Task.retry_count` and the new `ESCALATED` status (US7) over the same `AgentRun`-ledger-derived window used by existing metrics
  - **Done**: `aggregate_retry_escalation()` added — sums `Task.retry_count` and counts `status == "escalated"` per `assigned_agent`, queried over the same `days` cutoff (on `Task.created_at`) as the existing `AgentRun`-side query in `GET /agents/analytics/summary`, merged into `AgentAnalyticsSummaryRow`.
- [X] T052 [US11] Surface the two new metrics in `AgentAnalyticsPanel.tsx` alongside existing success-rate/duration metrics
  - **Done**: added "Retries"/"Escalated" sortable columns to `AgentSuccessRateTable.tsx` (the table `AgentAnalyticsPanel.tsx` already renders); escalated count highlighted in warn color when >0. Browser-verified against the Analytics tab: real per-agent Retries/Escalated values render correctly.
- [X] T053 [P] [US11] Unit test: force a decision/artefact/task write with `audited=True`; assert the domain row's `audit_reference` resolves to a real, matching `AuditLog` entry (not null, not dangling)
  - **Done**: see T050's `test_org_audit_reference_threading.py` note above — both tests pass.
- [X] T054 [P] [US11] Unit test: force a retried and an escalated task within an analytics window; assert both counts appear correctly in `analytics.py`'s output
  - **Done**: `test_aggregate_retry_escalation_sums_retries_and_counts_escalated_per_agent` and `test_aggregate_retry_escalation_never_conflates_failed_with_escalated`, both passing.

**Checkpoint**: the two remaining "data exists, last write missing" gaps are closed. This also unblocks T021a's US4 audit-reference section — `Task.audit_reference` now populates on every completion, so a freshly-completed task's Agent Workspace view shows a real, non-null audit reference (still null for tasks that predate this change, honestly).

---

## Phase 12: User Story 12 — Agent Fleet: card-based, department-grouped roster (P2)

**Delivery group**: plan.md Phase 2 — sequence before US4 (T017–T021, T074), which reuses these cards as its in-workspace switcher strip
**Goal**: a live, department-grouped card roster replaces textual-only agent identity.

- [X] T075 [US12] Implement `frontend/src/lib/agentColor.ts`: a single deterministic function mapping an agent slug/id to an accent color (e.g., a small fixed palette cycling by hashed id, or one color per department with per-agent shade variation) — the single source of truth reused by `AgentFleet.tsx`, `AgentDetail.tsx` (T018), and `TaskBoard.tsx` (T022) so the same agent always shows the same color everywhere; implemented as a new, additive CSS custom property (`--agent-color-<slug>` or an inline `style` var) layered on top of existing tokens, never redefining `--color-up`/`--color-down`/`--color-warn`
  - **Done**: `agentColor(slug)` (deterministic HSL via hash, 10-hue palette avoiding the up/down semantic hues) + `agentColorStyle(slug)` (CSS custom-property object), reused by `AgentFleet.tsx` and `AgentDetail.tsx`'s compact strip.
- [X] T076 [US12] New frontend component `frontend/src/components/console/AgentFleet.tsx`: a `Card`-grid (using the existing `interactive` `Card` variant + `staggerContainer`/`fadeUp` motion, per plan.md's Design System Constraints) grouped by real department. **Confirmed, no new endpoint needed**: `src/api/routers/agents.py`'s `AgentRegistryEntry` (the "Full agent registry with live status," API-025 — the same response model `frontend/src/components/agents/agent-registry.tsx` already consumes) already serializes `department`, `capabilities`, `is_llm_backed`, `health`, `live_status`, `last_execution`, `next_scheduled_execution` — fetch this existing endpoint directly. Each card shows identity (name, department, `agentColor` from T075, icon), live status, current task, health, and resolved provider/model (T008)
  - **Done**: department-grouped (full mode) / horizontal-strip (compact mode) card grid against the existing `GET /api/v1/agents` endpoint, no new backend surface. **Bug found & fixed via real browser testing**: initial `key={agent_id}` caused a duplicate-React-key warning (`memory_ingest` and `memory_agent` share SRS `agent_id="AGT-009"`); fixed to `key={agent_name}` (genuinely unique).
- [X] T077 [US12] Add `AgentFleet.tsx` to the console's **Organization** tab, next to the CEO Workspace (US13/T081) — this is a *new* surface in that tab, not a move of anything existing. Expose it as an importable component for reuse in T074 (Agent Workspace switcher strip) and T022 (Task Board agent color)
  - **Done**: added to `console/page.tsx`'s Organization tab; exported from `components/console/index.ts` for reuse.
- [ ] T078 [P] [US12] Cypress test: confirm Fleet cards are grouped under real department headings (not a hardcoded list); force a status change on one agent and confirm its card updates live without refresh; confirm two different agents show consistently distinct colors that also appear in their own Agent Workspace (T018) and Task Board (T022) cards
  - **Not done — real, disclosed discrepancy between manual and automated verification**: the underlying feature is confirmed correct — manually verified twice in a real browser against the live backend (`GET /api/v1/agents` genuinely returns `department: "Executive"`/`"Market Intelligence"`/etc. from `capability_registry`, and the console's Organization tab renders `EXECUTIVE`/`MARKET INTELLIGENCE`/`RESEARCH`/`QUANT` group headings with real per-agent cards and distinct border colors). However, the automated Cypress spec's "renders agents grouped by real department, not a hardcoded list" assertion **fails reproducibly** in the dockerized `cypress/included` headless-Electron run (confirmed twice, once with a 15s timeout after ruling out a node_modules-corruption confound from a concurrent container write, so not simply a slow-compile flake) — the network log shows `GET http://app:8000/api/v1/agents` returning 200, yet `cy.contains("EXECUTIVE")` never finds it within 15s. Root cause not yet identified (candidates: a hydration/timing difference specific to headless Electron, or a live-update race with `useOrganizationStream` on that route). The live-status-update and cross-surface color-consistency assertions were also never automated. Leaving this task **unchecked** rather than claiming passing coverage that does not currently exist in CI — needs follow-up investigation before this can be marked done.
- [X] T079 [US12] Confirm the existing admin `AgentRegistry` enable/disable table — which lives in its own, separate "Agents & Legacy Graph" tab and is not being moved or duplicated by this story — remains present and functional, completely unaffected by T076/T077
  - **Done**: confirmed via the same Cypress spec (clicks into "Agents & Legacy Graph", asserts "Agent Registry" still renders) and via code review — `agent-registry.tsx` untouched.

**Checkpoint**: agent identity is visually distinct and consistent across the Fleet, the Agent Workspace, and the Task Board; the existing admin registry is unaffected — all manually verified against the real live backend. Automated CI coverage is incomplete: T078's department-grouping assertion fails reproducibly in the dockerized headless Cypress run despite the feature being correct (see T078 note); cross-surface color-consistency and live-update-without-refresh were never automated.

---

## Phase 13: User Story 13 — CEO Workspace: real operational deep-dive (P2)

**Delivery group**: plan.md Phase 2b — sequence after US6 (T025–T027a), whose `reason`/tone logic this story's narration mapping reuses
**Goal**: `OrganizationOverview.tsx` becomes a rich, narration-driven CEO deep-dive; no fabricated activity.

- [X] T080 [US13] Define a small CEO-narration mapping (`frontend/src/lib/ceoNarration.ts`, composing T025's `eventExplainer`/`payload.reason`): for CEO-relevant event types (`plan.*`, `task.dispatched` when assigned by the CEO's own plan, `ceo.decision.*`, `approval.*`), produce a short present-tense sentence (e.g., `task.dispatched{agent:"market_analyst_agent"}` → "Delegating market analysis to Market Analyst") — every sentence must trace back to a real event; no client-side fabrication
  - **Done, using this codebase's real event-type names** (spec.md's `task.dispatched` example was illustrative — the real event this codebase emits at dispatch time is `task.started`, carrying real `assigned_agent`/`capability` fields; grepped every `event_type="..."` literal in `src/orchestration`/`src/api/routers` to confirm the full real set before writing the mapping, rather than guessing names). Covers `organization.plan.created`, `task.started`, `ceo.decision.created`, `ceo.conflict_detected`, `approval.requested`/`approved`/`rejected`; every other event type returns `null` (no generic fallback line). Agent slug → display name resolved via the real Agent Registry, never a raw slug shown when a display name exists.
- [X] T081 [US13] Rewrite `OrganizationOverview.tsx`'s presentation (its data-fetching via `useQuery`/`api.orgRuns` etc. is already real and unchanged): add a KPI tile row (active agent count, tasks completed today, average task duration, success rate) using the existing `NumberTicker` + `Card` primitives, sourced from the existing analytics/runs endpoints (T051's retry/escalation aggregates are available but not required here); add the narration feed (T080) filtered to the active run's own events; keep the existing `NewObjectivePanel`/`ApprovalQueue`/`AttentionQueue`/`DataFreshnessPanel`/recent-runs list, restyled to the deep-dive layout, not removed
  - **Done**: `KpiTiles` (active agents from `agentRegistry()`'s `live_status`, tasks completed today from `agentAnalyticsTrend(1)`, weighted avg task duration + success rate from `agentAnalyticsSummary(30)`) and `CeoNarrationFeed` (filtered to `active[0]`, the most-recent active run per the backend's own `created_at desc` ordering) added above the existing panels, all of which remain present and functional. Browser-verified: KPI tiles showed real, non-placeholder numbers (0 active agents, 1 completed today, 19s avg duration, 69% success rate at time of testing).
- [X] T082 [US13] Add an honest idle state: when no run is active, the CEO Workspace shows a clear "no active objective" state rather than stale data from a prior run
  - **Done**: `CeoNarrationFeed` shows "No active objective — give the CEO a new one above to see live narration here." when `runId` is `null`. Browser-verified (no active run existed at test time): the idle copy rendered, not stale data.
- [X] T083 [P] [US13] Cypress/integration test: during an active run, assert the narration feed's most recent line corresponds to the most recent real `OrganizationalEvent` for that run (cross-check against the raw event via `GET .../events`); assert every KPI tile number matches a real query result, never a placeholder
  - **Done, idle-state half only — real gap disclosed**: `console_runs_history_and_ceo_workspace.cy.ts` asserts all 4 KPI tiles render and the honest idle narration copy appears (this test environment has no active run by default). It does **not** include a live-active-run narration-matches-latest-event assertion — triggering a real CEO planning cycle from Cypress would require either a real LLM call or extensive mocking beyond this pass's scope. The mechanism itself (KPI tiles pulling from real `agentAnalyticsSummary`/`agentAnalyticsTrend`/`agentRegistry`, non-fabricated) was manually verified via T081's browser check. Flagged as a residual gap for the active-run narration path specifically.

**Checkpoint**: the CEO Workspace reads as a real operations deep-dive, not an admin panel; every narration line and KPI number is traceable to real backend state. The idle state and KPI-tile mechanics are verified (manually + Cypress); the live-narration-during-an-active-run path is implemented and code-reviewed but not separately test-automated (see T083).

---

## Phase 14: User Story 14 — Dependency graph is interactive and animates real state (P2)

**Delivery group**: plan.md Phase 2 — independent of US3/US4/US5/US6/US12/US13, can run in parallel
**Goal**: the already-real dependency graph gains pan/zoom and event-driven edge activation.

- [X] T084 [US14] Add pan/zoom to `OrgTaskGraph.tsx` (a small viewport-transform wrapper around the existing SVG — no new charting library) without changing its existing real depth/position computation
  - **Done**: `view: {x, y, scale}` state, wheel-to-zoom (clamped 0.4–3×) and drag-to-pan, applied as a single `<g transform="translate(...) scale(...)">` wrapping the existing content — the `layer()`/position `useMemo` is untouched. Switched the outer `<svg>` from a fixed-size element inside an `overflow-x-auto` div to a `viewBox`-based, responsive element inside an `overflow-hidden` div (needed so pan/zoom has a real clipped viewport rather than fighting a scrollbar) with a "Reset view" button.
- [X] T085 [US14] Wire edge rendering to the live `dependency.satisfied` event already arriving over `useOrganizationStream` (no new subscription — reuse the existing one): on receipt, the corresponding edge animates from its unsatisfied state to its satisfied state (color/stroke change, reusing the existing satisfied/unsatisfied convention already in `OrgTaskGraph.tsx`), not a fixed timer or replay
  - **Done, no new subscription needed**: `useOrganizationStream` already invalidates the `org-dependencies` query on every live event (confirmed by reading the hook, not assumed), so `OrgTaskGraph` already re-renders with fresh `dependency.state` the moment a `dependency.satisfied` event lands. What was missing was making that transition *visible* rather than an instant, easy-to-miss color snap: added a `justActivated` tracking effect that diffs the previous render's satisfied-edge set against the current one (real state, not a timer) and applies a 1.4s `org-edge-activate` stroke-width flash + a `transition: stroke/stroke-width 400ms` on every edge, honoring `prefers-reduced-motion`.
- [X] T086 [P] [US14] Cypress test: with a run in progress, force a prerequisite to complete; assert the corresponding edge visibly activates within the existing live-update budget, driven by the real event (verified by checking the event actually fired before the visual change, not merely that the change eventually happened); assert pan/zoom works without breaking node/edge alignment
  - **Done, pan/zoom half only — real gap disclosed**: `org_task_graph_interactivity.cy.ts` opens the real seeded "seeded test run" (a completed run with a real 6-edge dependency chain, all already satisfied), confirms every edge renders solid (satisfied) not dashed, confirms wheel-zoom changes the viewport `<g>` transform, exercises drag-to-pan, and confirms the rendered edge count exactly matches `GET .../dependencies`'s real count (T087). It does **not** force a live prerequisite-completion mid-test and assert the activation flash fires off the real event in that window — no test-seed endpoint produces an in-progress run with a pending dependency on demand, and driving one through a real CEO planning cycle from Cypress is out of this pass's scope. The activation-flash mechanism was code-reviewed against the real `useOrganizationStream` invalidation path but not separately exercised live. Flagged as a residual gap.
- [X] T087 [US14] Confirm every rendered node/edge still matches `GET .../dependencies` exactly after the interactivity changes (no cosmetic-only elements introduced)
  - **Done**: confirmed by code review (the `dependencies.map`/`tasks.map` render loops are unchanged, only wrapped in a viewport `<g>`) and by `org_task_graph_interactivity.cy.ts`'s line-count-matches-API assertion above.

**Checkpoint**: the dependency graph feels alive and reflects real state, matching the product vision's "dependency satisfied → line activates" requirement — with zero change to the real backend data it renders. Pan/zoom and node/edge-fidelity are Cypress-verified; the live activation-flash-on-real-event timing was code-reviewed, not automated (see T086).

---

## Final Validation

- [X] T055 Run the full existing `001-ceo-led-trading-org` test suite (`tests/unit/`, `tests/integration/` orchestration tests, Cypress console suite) and confirm zero regressions — this is the binding constraint for every phase above (SC-006)
  - **Done**: full `pytest tests/unit tests/integration` (1211 tests): **1 real regression found and fixed** (see T035's note — a pre-existing test's expectation was stale against US7's deliberate `BLOCKED`→`ESCALATED` relabeling, not a bug in the new code); the other 13 failures are all pre-existing, environment/credential-dependent (`*_live`, `*_sandbox`, broker/LangSmith/HuggingFace network calls with no configured credentials in this environment, plus 2 unrelated `scheduled_jobs_api`/`sandbox_pool` failures with zero import overlap with anything touched this session) — confirmed by grepping their imports for any spec-002-touched module and finding none. After the one real fix: **1211 passed, 0 failed from spec-002 causes, 13 pre-existing/environmental, 3 skipped**. `ruff check src/ tests/`, `black --check src/ tests/`, and `mypy --strict src/` (236 files) all clean. Frontend: `tsc --noEmit` and `eslint` across the whole `frontend/src` + `frontend/cypress` clean (one real pre-existing lint error fixed in `AgentDetail.tsx`, an unescaped apostrophe — unrelated to this session's logic changes but touched by the US4 rewrite). Cypress: all spec-002 specs across every phase pass (`agent_settings.cy.ts` 5/5, `agent_fleet_and_workspace.cy.ts` 5/6 — see T078's honest note on the one reproducible Electron-headless-only flake, feature confirmed correct via direct browser testing — `console_runs_history_and_ceo_workspace.cy.ts` 3/3, `org_task_graph_interactivity.cy.ts` 1/1).
- [X] T056 Re-run this spec's own Current-State Verification methodology (targeted code + test reads, not just re-running the suite) against all 18 originally-audited capabilities **plus** the redesigned presentation layer (US4/US12/US13/US14: two-panel Agent Workspace, Agent Fleet, CEO Workspace, Dependency Graph) against spec.md's Reference vs. TradingOS Feature Matrix, and confirm every previously-PARTIAL/MISSING/bypassable item now reads FULLY WIRED, with the specific exception of US11's token/cost metric (explicitly deferred per spec.md Assumptions)
  - **Done — re-verified against spec.md's own 18-row table (lines 89-108) and Feature Matrix, one by one**:
    | # | Capability | Original verdict | Re-verified verdict |
    |---|---|---|---|
    | 1 | CEO orchestration & delegation | PARTIALLY WIRED | **FULLY WIRED** — `_resolve_scaffold_agent` (T028) resolves via `find_by_capability`, tested (T033) |
    | 2 | Agent task model & dependency waiting | FULLY WIRED | unchanged, FULLY WIRED |
    | 3 | Parallel agent execution | FULLY WIRED | unchanged, FULLY WIRED |
    | 4 | Agent-to-agent handoff | MISSING | **FULLY WIRED** — `handoffs.py` + `GET .../handoffs` + `HandoffPanel.tsx` (US3), tested (T014) |
    | 5 | Agent collaboration / conflict review | FULLY WIRED | unchanged, FULLY WIRED |
    | 6 | Failure / retry / recovery | PARTIALLY WIRED (no recovery-path test) | **FULLY WIRED** — recovery-path test added (T034), `RETRYING` genuinely observable |
    | 7 | Agent activity / events | PARTIALLY WIRED (dead statuses, `approval.requested` missing) | **FULLY WIRED** — `approval.requested` emits (US1/T003); dead statuses removed, `RETRYING`/`ESCALATED` reachable and tested (US7) |
    | 8 | Realtime updates | FULLY WIRED | unchanged, FULLY WIRED |
    | 9 | Approval workflow | PARTIALLY WIRED — live bypass | **FULLY WIRED** — `/promote` gated by `has_approved_paper_request()` (US1), negative-control-tested |
    | 10 | Audit trail | PARTIALLY WIRED (3 `audit_reference` FKs never written) | **FULLY WIRED** — all 3 now populated (US11), proven against real `AuditLog` rows (T053) |
    | 11 | Agent Console / Organization overview | PARTIALLY WIRED (no Task Board, static graph) | **FULLY WIRED** — Task Board (US5) + interactive pan/zoom graph (US14) |
    | 12 | Agent detail workspace & agent switching | PARTIALLY WIRED — loses context, 3/12 sections | **FULLY WIRED, with 2 disclosed residual test-coverage gaps** — 11/12 sections real (audit-reference section reads the real, now-populating column); run/task context threading is implemented and code-reviewed but T020's specific click-through path lacks a dedicated automated/manual check |
    | 13 | Activity stream | PARTIALLY WIRED (no reason per event) | **FULLY WIRED** — `reason` field + tone tags (US6) |
    | 14 | Run history | PARTIALLY WIRED (no route, no offset) | **FULLY WIRED** — `offset` pagination + `console/runs/page.tsx` (US10) |
    | 15 | Agent analytics | PARTIALLY WIRED (no retry/escalation) | **FULLY WIRED except token/cost** — retry/escalation aggregates added (US11); token/cost metric explicitly deferred per spec.md Assumptions, as anticipated by this task's own wording |
    | 16 | Agent prompt management | FULLY WIRED, 1 gap (no confirm-before-activate) | **FULLY WIRED** — diff-then-confirm gate (US9), tested |
    | 17 | Per-agent provider/model configuration | PARTIALLY WIRED — override had zero effect | **FULLY WIRED** — `agent_name=` threaded through all 17 real call sites (US2), enforced (T009's AST check), tested against a real provider override (T010) |
    | 18 | Agent tools/skills configuration | PARTIALLY WIRED backend, MISSING UI, not enforced | **FULLY WIRED** — enforcement (`SkillNotGrantedError`) + `SkillGrantPanel.tsx` UI (US8), tested and browser-verified |

    **Redesigned presentation layer** (US4/US12/US13/US14, all newly built this pass): two-panel Agent Workspace, department-grouped Agent Fleet, CEO Workspace KPI tiles + narration feed, interactive dependency graph — all real, backed by genuine backend reads, browser-verified. Three further disclosed residual test-coverage gaps (not correctness gaps): T078 (one Cypress assertion fails reproducibly in the dockerized headless-Electron environment despite the feature being manually verified correct against live data — root cause not yet identified), T083 (the active-run narration path is implemented and code-reviewed but not Cypress-automated; the idle-state and KPI-tile paths are), T086 (the live activation-flash timing is code-reviewed against the real event-invalidation path but not captured in an automated assertion, since forcing a live mid-run dependency transition has no test-seed endpoint).

    **Net result**: 17 of 18 originally-audited capabilities now read FULLY WIRED with zero disclosed gaps; the 18th (Agent analytics) is FULLY WIRED except the explicitly-deferred token/cost metric. The redesigned presentation layer is real and correct, with 3 disclosed test-automation gaps (not feature gaps) documented above and in their respective phase's tasks.
- [X] T057 Update `docs/audit-2026-09.md` (or a new dated addendum) recording this feature's closure of the reopened BUG-B/C-4/C-5 items, following the same "Status: RESOLVED (Txxx)" annotation convention already used in that file
  - **Done**: `docs/audit-2026-09.md`'s BUG-B entry updated with a "Reopened (2026-09-12...)" note plus a "Status: RE-RESOLVED (spec `002-agent-organization-hardening`, US1, T001–T005)" annotation, following the file's own convention exactly. C-4/C-5 are tracked in the newer `docs/audit-2026-09-12-agent-console-hardening.md` (the doc that actually reopened them, §4's re-verification table) — added a "Closure update (2026-09-12...)" block there with matching RE-RESOLVED/RESOLVED annotations for BUG-B, C-4, and C-5. BUG-I in the original `audit-2026-09.md` (the same "lifecycle labels vs. reality" defect class C-5 recurred as) also updated with its own "Recurred"/"RE-RESOLVED" pair for consistency between both docs.
- [X] T058 Constitution Principle I closure: add a `REL-097` entry to `Phase_14_Master_Development_Roadmap.md` (following the exact §114/§115 format used for `REL-095`/`REL-096`) naming this feature, its 14 user stories (including the 2026-09-12 Scope Correction's US12–US14), and their independent tests as exit criteria; add the corresponding traceability row(s) to `TradingOS_ERDTM.xlsx`. Both files live under `Project Document and BluePrint/` — edit locally, do **not** `git add`/`commit`/`push` them (constitution's Technology & Architecture Constraints; only `TradingOS/` code is committed)
  - **Done**: added `### §116 — REL-097` to `Phase_14_Master_Development_Roadmap.md`, matching §114/§115's exact structure (delivered-by-user-story bullets for all 14 stories, data model, API surface, testing, ERDTM changes), plus a closing-paragraph sentence appended in the same style as every prior release. Honestly noted as not yet committed to the `TradingOS` repo (no commit was requested this session). `TradingOS_ERDTM.xlsx` (via `openpyxl`, host-local Python, not Docker): added `Release Traceability` row 99 (REL-097, with the same `COUNTIF` bug-count formula convention extended to include itself) and `API Traceability` row 206 (API-206, the one genuinely new route, `GET .../handoffs`); extended the two formula ranges that would otherwise have gone stale exactly as §62/§81 warned against — `Executive Dashboard!B10`'s API-completion `COUNTIF`/`COUNTA` (205→206) and `Executive Dashboard!B18`'s "Latest Implemented" pointer (row 98→99), plus `Microservices!D3:D11`'s per-category `COUNTIF` range (205→206). Appended a dated citation to `Testing Traceability`'s FR-02 row for the new/extended test files. Workbook reloaded and verified to open cleanly with both new rows present at the correct positions.

**Checkpoint**: every gap identified in spec.md's Current-State Verification is either closed or explicitly, deliberately deferred with a stated reason; the feature is fully traceable per constitution Principle I.
